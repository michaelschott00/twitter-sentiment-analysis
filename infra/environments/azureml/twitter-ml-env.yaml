$schema: https://azuremlschemas.azureedge.net/latest/environment.schema.json
name: twitter-ml-env
description: Training/inference environment for the twitter-sentiment project (see docs/azure-infrastructure-plan.md section 7.1).
build:
  # Repo root: the Dockerfile installs ./twitter from pyproject.toml.
  path: ../../..
  dockerfile_path: infra/environments/azureml/Dockerfile
